#maps-list

  %span#map-list-count= "Showing #{pluralize(@maps.size, 'map')}"

  .grid-container.flex-center{ data: { 'controller': "header", 'action': "click->header#hideNavDropdown" } }
    - @maps.each do |map|
      = render map, rw: false, last_change: false, avatar: false, delete: false
