- content_for :title, "Mapforge: Recently watched and My maps"

#maps

  - if @recent_map_ids.present?
    %h1.mapforge-font Recently viewed Maps
    .grid-container.flex-center.mb-4
      - @recent_map_ids.each do |map_id|
        - recent_map = Map.unscoped.find_by(id: map_id)
        - if recent_map.present?
          = render recent_map, rw: true
        - recent_map = Map.unscoped.find_by(public_id: map_id)
        - if recent_map.present?
          = render recent_map, rw: false


  %h1.mapforge-font 
    Your Maps 
    %span.fs-5
      (#{@my_maps.size})
  .grid-container.flex-center
    - @my_maps.each do |map|
      = render map, rw: true, last_change: false, avatar: false, delete: true

  - if @my_maps.empty?
    .container.pt-4.pb-2.mt-4.mb-4.empty-map-list.background-bright
      %p.text-center
        In this gallery, your personal maps will be shown.
        %br
        You can try out the 
        = link_to 'tutorial map', demo_path, class: 'btn btn-orange call-to-action', data: { turbo_method: :post }
        to learn how Mapforge works, or you can       
        = link_to 'create', create_map_path, class: 'btn btn-orange call-to-action', data: { turbo_method: :post }
        your first map right away.